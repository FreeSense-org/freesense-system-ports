# FreeSense owns network state in config.xml.  Preserve datasource network
# metadata for the adapter, but never let cloud-init create competing rc.conf
# entries or activate interfaces itself.
network:
  config: disabled
disable_network_activation: true
ssh_pwauth: false
users: []
datasource_list: [ NoCloud, ConfigDrive, OpenStack ]
